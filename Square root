int sqrtN(long long int N)
{
    // Write your code here.
    int s=0;
    unsigned int e=N;
    int ans=-1;
    
    while(s<=e){
        long long int mid=s+(e-s)/2;
        long long int sq=mid*mid;
        if(sq==N){
            return mid;
        }
             if(sq<N){
                ans=mid;
                s=mid+1;
            }
            else{
                e=mid-1;
            }
        }
    return ans;
    }
