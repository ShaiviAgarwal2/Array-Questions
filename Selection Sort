#include<iostream>
using namespace std;

void selection(int arr[],int n)
{
int swap=0;
for(int i=0;i<n-1;i++)
{
int minindex=i;
for(int j=i=1;j,n;j++)
{
if(arr[minIndex]>arr[j])
{
minindex=j;
}
}
swap=arr[minIndex];
arr[minIndex]=arr[i];
arr[i]=swap;
}
}
